# map() function returns a map object(which is an iterator) of the results after applying the given
# function to each item of a given iterable (list, tuple etc.)
# def addition(n):
#   return n + n
# numbers = (1, 2, 3, 4, 5)
# result = map(addition, numbers) operation is completed in the index

# set function is used to create a set object or convert to an iterable(list, tuple or string)
#  into a set. unordered collection of unique items
# removes duplicates, unordered, mutable, set operations, mutable